# for local test, upon deploy GCP doesn't permit us to set GCP_PROJECT, so we alter the name to get around that for local testing
#LOCAL_GCP_PROJECT             : saas-rally-dev-integrations

# Google Pub Sub related items used by the catch, pan and post functions
GCP_ZONE                      : us-central1
LOCATION_ID                   : global

# DB related items only needed for installation, panner, and poster functions
GCLOUD_SQL_INSTANCE           : atoms-hackathon
DB_NAME                       : kingfisher
DB_USER                       : postgres
DB_PASSWORD                   : hackathon

# local cloud sql proxy directory where the unix 
CLOUD_SQL_DIR                 : /Users/pairing/temp/cloudsql

# GCP PubSub topics and subscriptions
KF_OCM_EVALUATE      : kf-ocm-evaluate
KF_OCM_EVALUATE_SUB  : kf-ocm-evaluate-sub

KF_WEBHOOK_NOGO      : kf-webhook-nogo
KF_WEBHOOK_NOGO_SUB  : kf-webhook-nogo-sub

KF_WEBHOOK_READY     : kf-webhook-ready
KF_WEBHOOK_READY_SUB : kf-webhook-ready-sub

KF_WEBHOOK_FIRED     : kf-webhook-fired
KF_WEBHOOK_FIRED_SUB : kf-webhook-fired-sub


